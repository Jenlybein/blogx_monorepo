# API 文档目录

这里集中放置 API 相关的非源码资料，原先位于 `apps/api/.read` 的内容已迁移到这里和 `openapi/api`。

## 目录说明

- `records/`：后端开发记录、长版说明文档。
- `architecture/`：架构示意图、Mermaid 草图。
- `tasks/`：待办、重构方向和演进清单。
- `monorepo-layout.md`：当前 API 在 monorepo 中的目录布局说明。

## OpenAPI

OpenAPI 描述文件已迁移到 `openapi/api/myblogx.openapi.json`，与业务文档分开管理。
