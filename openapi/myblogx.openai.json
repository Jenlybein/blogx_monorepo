{
  "name": "myblogx read api guide",
  "updated_at": "2026-04-10",
  "purpose": "说明当前高频读取接口的推荐调用方式，以及请求进入后端后依赖是如何通过 AppContext 注入下去的。文章读取统一走 /api/search/articles，评论、关注、收藏夹、置顶、聊天会话虽然外部路径不变，但内部已切到专用 query service、统一计数读层和显式依赖注入。",
  "important_changes": [
    "GET /api/articles 已下线，不再用于文章读取。",
    "文章列表、文章搜索、作者文章、我的文章、管理员文章查询统一走 GET /api/search/articles。",
    "评论、关注、收藏夹、置顶、聊天会话这几条高频读取链路，内部已经不再走 handler + ListQuery + preload + Redis 拼装。",
    "文章计数和评论计数都通过统一 reader 补偿，外部调用方不需要自己处理 Redis 增量。",
    "评论、关注、收藏关系、聊天会话等高频展示字段已经写入冗余快照；昵称、头像、文章标题、封面、状态变更后会同步刷新。",
    "API 启动层现在会构造 AppContext，并通过 Gin 中间件注入到请求上下文；高频 handler / query service / 鉴权链路优先从 AppContext 读取 DB、Logger、Config。",
    "router 层不再依赖 api.App 静态单例，而是显式接收 api.New(ctx) 组装出的 API 实例。"
  ],
  "dependency_injection_usage": {
    "summary": "现在推荐把依赖注入理解为三层：启动层组装、路由层传递、handler/service 按需读取。",
    "how_request_flows": [
      "程序启动时，在 cmd/server/main.go 初始化 Config、Logger、DB、Redis、ESClient 等资源。",
      "这些资源会被组装成 AppContext。",
      "router.Run(ctx, api.New(ctx)) 会把 AppContext 放入 Gin 中间件，同时把 API 实例显式传给各个 Router。",
      "进入 handler 后，推荐通过 mustApp(c) 或 appctx.MustFromGin(c) 读取 DB、Logger、Config，而不是再直接访问 global.DB / global.Logger / global.Config。",
      "进入鉴权或 query service 后，依赖由构造函数显式传入，例如 NewQueryService(db) 或 AuthenticatorFromGin(c)。"
    ],
    "recommended_rules": [
      "新增 handler 时，优先使用 mustApp(c).DB / Logger / Config。",
      "新增查询服务时，构造函数必须显式接收 db，不要在 service 内部回退 global.DB。",
      "新增路由时，不要继续从 api.App 取模块，直接使用 router 传入的 app api.Api。",
      "只有启动装配层和 appctx 兼容兜底层允许感知 global.*；业务入口层不要继续扩散全局依赖。"
    ],
    "compatibility_note": "为了兼容仍直接调用 handler 的旧测试，appctx.MustFromGin(...) 当前保留了一层集中兜底：当 Gin 上下文里没有 AppContext，但全局资源已初始化时，会临时用全局资源构造 AppContext。新代码不要依赖这层兼容路径。"
  },
  "article_read": {
    "endpoint": {
      "method": "GET",
      "path": "/api/search/articles"
    },
    "auth": {
      "type_1_public": "匿名可访问",
      "type_2_recommend": "匿名可访问，登录后会叠加偏好标签推荐",
      "type_3_author": "匿名可访问，但必须传 author_id",
      "type_4_self": "必须登录",
      "type_5_admin": "必须管理员"
    },
    "query_parameters": {
      "type": {
        "type": "integer",
        "default": 1,
        "enum": {
          "1": "公共文章列表/公共搜索",
          "2": "猜你喜欢/推荐流",
          "3": "作者文章",
          "4": "当前登录用户自己的文章",
          "5": "管理员文章查询"
        }
      },
      "sort": {
        "type": "integer",
        "default": 1,
        "enum": {
          "1": "默认排序",
          "2": "最新发布",
          "3": "最多回复",
          "4": "最多点赞",
          "5": "最多收藏",
          "6": "最多浏览"
        }
      },
      "key": {
        "type": "string",
        "description": "主要搜索内容。为空时表示列表查询，不做关键词检索。"
      },
      "page": {
        "type": "integer",
        "default": 1,
        "description": "页码，从 1 开始。"
      },
      "limit": {
        "type": "integer",
        "default": 10,
        "description": "每页条数，建议 1-100。"
      },
      "page_mode": {
        "type": "string",
        "default_rule": "type=1/2/3 默认 has_more；type=4/5 默认 count。",
        "enum": {
          "has_more": "前台列表/滚动加载",
          "count": "后台准确数据页"
        }
      },
      "author_id": {
        "type": "string",
        "description": "作者 ID。type=3 时必填。"
      },
      "category_id": {
        "type": "string",
        "description": "分类 ID，所有 type 都支持。"
      },
      "tag_ids": {
        "type": "string",
        "description": "标签 ID 列表。推荐 tag_ids=1&tag_ids=2，也兼容 tag_ids=1,2。"
      },
      "status": {
        "type": "integer",
        "description": "继续传数字状态。type=1/2/3 只允许已发布状态；type=4/5 可按数字状态过滤。"
      }
    },
    "usage_rules": [
      "想查公共文章流：传 type=1，key 可空，前台默认建议 page_mode=has_more。",
      "想做站内搜索：仍然传 type=1，再额外传 key。",
      "想查某个作者的文章：传 type=3，必须传 author_id。",
      "想查自己的文章：传 type=4，并带登录态；status 继续传数字。",
      "想查后台审核池或后台文章管理：传 type=5，建议 page_mode=count。",
      "category_id 和 tag_ids 是统一筛选字段；新调用不要再继续使用 user_id。",
      "tag_ids 推荐写法是 tag_ids=1&tag_ids=2；如果前端不方便，也兼容 tag_ids=1,2。"
    ]
  },
  "high_frequency_read_apis": [
    {
      "name": "一级评论列表",
      "method": "GET",
      "path": "/api/comments",
      "auth": "匿名可访问",
      "query": {
        "article_id": "必填",
        "page": "页码，从 1 开始",
        "limit": "每页条数"
      },
      "response_rule": "返回 data.list + data.has_more。digg_count / reply_count 已自动叠加 Redis 增量，is_digg / relation 也会自动补齐。"
    },
    {
      "name": "二级评论列表",
      "method": "GET",
      "path": "/api/comments/replies",
      "auth": "匿名可访问",
      "query": {
        "article_id": "必填",
        "root_id": "必填，一级评论 ID",
        "page": "页码，从 1 开始",
        "limit": "每页条数"
      },
      "response_rule": "返回 root_id、reply_count、list、has_more。reply_count 已自动叠加 Redis 增量。"
    },
    {
      "name": "关注列表",
      "method": "GET",
      "path": "/api/follow",
      "auth": "必须登录",
      "query": {
        "user_id": "可选。不传时查自己；传别人时会校验对方是否公开关注列表。",
        "followed_user_id": "可选，按关注对象过滤",
        "page": "页码",
        "limit": "每页条数"
      },
      "response_rule": "返回 list + count。昵称、头像、简介和 relation 已由专用 query service 组装。"
    },
    {
      "name": "粉丝列表",
      "method": "GET",
      "path": "/api/fans",
      "auth": "必须登录",
      "query": {
        "user_id": "可选。不传时查自己；传别人时会校验对方是否公开粉丝列表。",
        "fans_user_id": "可选，按粉丝过滤",
        "page": "页码",
        "limit": "每页条数"
      },
      "response_rule": "返回 list + count。昵称、头像、简介和 relation 已由专用 query service 组装。"
    },
    {
      "name": "收藏夹列表",
      "method": "GET",
      "path": "/api/articles/favorite",
      "auth": "type=1 需要登录；type=2 允许公开访问；type=3 需要管理员",
      "query": {
        "type": {
          "1": "查自己的收藏夹",
          "2": "查别人的收藏夹",
          "3": "管理员后台查"
        },
        "user_id": "type=2 时必填",
        "article_id": "可选。type=1 时可用来判断某篇文章是否已在该收藏夹中",
        "key": "可选，按收藏夹标题筛选",
        "page": "页码",
        "limit": "每页条数"
      },
      "response_rule": "返回 list + count。article_count 直接来自读模型快照并带兼容兜底；has_article 只在 type=1 且传 article_id 时有意义。"
    },
    {
      "name": "收藏夹内容列表",
      "method": "GET",
      "path": "/api/articles/favorite/contents",
      "auth": "收藏夹所有者和管理员可直接访问；其他人只有收藏夹公开时可访问",
      "query": {
        "favorite_id": "必填",
        "key": "可选，按文章标题筛选",
        "order": "可选，created_at desc 或 created_at asc",
        "page": "页码",
        "limit": "每页条数"
      },
      "response_rule": "返回 list + count。标题/摘要/封面/作者来自收藏关系快照并带文章表兜底；view/digg/comment/favor 已自动补 Redis 增量。"
    },
    {
      "name": "置顶文章列表",
      "method": "GET",
      "path": "/api/articles/top",
      "auth": "匿名可访问",
      "query": {
        "type": {
          "1": "作者置顶",
          "2": "管理员置顶"
        },
        "user_id": "type=1 必填"
      },
      "response_rule": "返回 list + count。内部已去掉 SQL JOIN，按文章、作者、分类、标签分步装配；同一篇管理员置顶文章会自动去重。"
    },
    {
      "name": "聊天会话列表",
      "method": "GET",
      "path": "/api/chat/sessions",
      "auth": "必须登录",
      "query": {
        "type": {
          "1": "查自己的会话",
          "2": "管理员代查指定 user_id 的会话"
        },
        "user_id": "type=2 时必填",
        "page": "页码",
        "limit": "每页条数"
      },
      "response_rule": "返回 list + count。对方昵称/头像已写入会话快照并带用户表兜底；relation 由专用 query service 统一计算。"
    }
  ],
  "compatibility": {
    "still_supported_temporarily": [
      "user_id -> author_id（仅文章搜索兼容）",
      "tag_list -> 旧 AI 搜索兼容，仅旧流程使用"
    ],
    "recommended_now": [
      "author_id",
      "tag_ids",
      "page_mode"
    ]
  }
}
